🎮 Glomy Garden - Production Ready Farming RPG

โปรเจกต์เกมแนว Farming RPG ที่พัฒนาด้วย Next.js (App Router), TypeScript, และ Prisma โดยเน้นประสิทธิภาพการทำงานที่ 60FPS บน Canvas Engine และระบบ Backend ที่ตรวจสอบความถูกต้องแบบ Server-Authoritative

🚀 Quick Start (Local Development)

1. ติดตั้ง Dependencies

npm install


2. ตั้งค่า Environment Variables

สร้างไฟล์ .env และระบุ DATABASE_URL (PostgreSQL):

DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"


3. เตรียมฐานข้อมูล (Prisma)

ทำการ Migrate โครงสร้าง Database และสร้าง Client:

npx prisma migrate dev --name init
npx prisma generate


4. ลงข้อมูลเริ่มต้น (Seed)

เพิ่มข้อมูลพืช (Crops) และ AI Master เข้าสู่ระบบ:

npx prisma db seed


5. รันโปรเจกต์

npm run dev


เปิดบราวเซอร์ไปที่ http://localhost:3000

🏗️ Production Deployment (Vercel)

Database: เตรียม PostgreSQL Database (แนะนำ Vercel Storage หรือ Supabase)

Environment Variables: เพิ่ม DATABASE_URL ใน Vercel Dashboard

Build Command: Vercel จะรัน npm run build อัตโนมัติ (ซึ่งจะทำการ prisma generate ตามที่ตั้งไว้ใน package.json)

Post-Deployment: รัน Seed script ผ่าน Vercel CLI หรือระบบจัดการฐานข้อมูลเพื่อให้มีข้อมูลพืชในระบบ

🛠️ Game Engine Architecture

Frontend: ใช้ requestAnimationFrame ร่วมกับ custom GameRenderer เพื่อประสิทธิภาพสูงสุด

State: จัดการข้อมูลผู้เล่นด้วย Zustand รองรับการทำ UI Overlay ทับบน Canvas

Security: ทุกการกระทำ (Action) จะถูกส่งไปคำนวณและตรวจสอบเงื่อนไขที่ Server API เพื่อป้องกันการแก้ไขข้อมูลฝั่ง Client (Anti-Cheat)

Sync: ระบบ Offline Calculation จะคำนวณการเจริญเติบโตของพืชย้อนหลังจาก lastUpdateAt เมื่อผู้เล่นกลับมาเข้าเกม

📁 Folder Structure

/engine: หัวใจของระบบกราฟิก (Renderer)

/lib/game-logic.ts: สูตรคำนวณที่ใช้ร่วมกันทั้งหน้าบ้านและหลังบ้าน

/app/api: ระบบ API สำหรับการ Sync และจัดการ Game Action

/hooks: ระบบควบคุม Loop การทำงานของเกม

📝 บันทึกเพิ่มเติมสำหรับ Developer

Emoji Mode: ระบบจะใช้ Emoji เป็นหลักหากไม่พบ Sprite sheet ในโฟลเดอร์ /public/sprites

Performance: มีการจัดการ Memory ผ่านการใช้ Singleton สำหรับ Prisma และการจัดการ Reference ใน Game Loop เพื่อป้องกัน Memory Leak